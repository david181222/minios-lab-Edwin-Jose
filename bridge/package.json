{
  "name": "minios-bridge",
  "version": "1.0.0",
  "description": "Bridge entre miniOS scheduler (named pipe) y dashboard (WebSocket)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
